<div class="container">
  <h1 class="text-center">Résumé du paiment</h1>

  <div class="row">
    <div class="overview">
      <div class="list-orders-card">
        <div class="table-margin">
          <table class="table">
            <thead>
              <tr>
                <th>Service</th>
                <th>Prix</th>
                <th>Status</th>

              </tr>
            </thead>
            <tbody>
              {{#each orders}}
              <tr>
                <td><img class="order-image-my-gig" src="{{ gig.picture }}">
                  <span>{{ gig.title }}</span>
                </td>
                <td>{{ gig.price }}€</td>
                <td><button class="btn btn-warning">En cours</button></td>
                <td><a href="/users/{{ seller._id }}/orders/{{ _id }}" class="btn btn-primary">Chatter avec le vendeur</a></td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>